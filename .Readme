 cd backend && npm i && npm run seed        # seed data so initial data will be created
  npm run dev         # server :5000 will start

  Terminal 2 — Frontend:
  cd frontend && npm i
  npm run dev         # :5173 will get open
