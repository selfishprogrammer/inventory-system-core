## Getting Started

**Terminal 1 — Backend**

```bash
cd backend && npm i && npm run seed && npm run dev
```

Seed data will be created and server will start on `:5000`

**Terminal 2 — Frontend**

```bash
cd frontend && npm i
npm run dev
```

Opens on `:5173`
